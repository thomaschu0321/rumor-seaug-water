# ============================================================================
# Requirements for Rumor Detection FYP Project
# Compatible with: RTX 3060 Ti, CUDA 12.6, Python 3.11
# ============================================================================
#
# IMPORTANT: Install PyTorch with CUDA 12.6 support FIRST before installing
# other packages. Use this command:
#
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#
# Then install remaining packages:
#   pip install -r requirements.txt
#
# Verify installation:
#   python -c "import torch; print('CUDA available:', torch.cuda.is_available()); print('CUDA version:', torch.version.cuda)"
#
# ============================================================================

# Core Deep Learning Framework (PyTorch)
# NOTE: Install PyTorch separately with CUDA 12.6 support first (see above)
# These version constraints ensure compatibility after PyTorch is installed
torch>=2.4.0

# Graph Neural Networks (PyTorch Geometric)
# Requires: torch, torch-scatter, torch-sparse, torch-cluster, torch-spline-conv
torch-geometric>=2.5.0

# Transformers and Language Models
# Used for: BERT feature extraction (bert-base-uncased)
#           Sentence transformer models (all-MiniLM-L6-v2 for node augmentation)
#           Note: Models are loaded via transformers.AutoModel, not sentence-transformers library
transformers>=4.40.0

# Data Processing and Scientific Computing
numpy>=1.26.0,<2.0.0  # Python 3.11 compatible, <2.0 for stability
scikit-learn>=1.4.0  # Used for: DBSCAN clustering, metrics, StandardScaler

# Utilities
tqdm>=4.66.0  # Progress bars

# LLM Integration (Azure OpenAI API)
# Used for: Node-level text augmentation via Azure OpenAI
openai>=1.40.0

# Environment Configuration
# Used for: Loading .env file with API keys
python-dotenv>=1.0.0

# ============================================================================
# Additional PyTorch Geometric Dependencies
# These are automatically installed with torch-geometric, but listed for reference:
# - torch-scatter
# - torch-sparse  
# - torch-cluster
# - torch-spline-conv
# ============================================================================
